'use strict';

/**
 * 
 * @param {Array} sendTo List email need to send
 * @param {String} title Title of the email
 */
exports.sendEmail = (sendTo, title, content) => {
  try {
    
  } catch (error) {
    
  }
}